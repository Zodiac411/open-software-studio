# Design

`UX-001`: one focused screen: task input, add action, empty state, list, completion toggle. Keyboard submit works; completed tasks retain visible completion state. Use intentional compact hierarchy, not dashboard cards.
