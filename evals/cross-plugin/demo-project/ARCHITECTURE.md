# Architecture

`ADR-001`: use a dependency-free browser module with in-memory state because the demo's non-goals exclude persistence. `TASK-001` owns task creation; `TASK-002` owns completion. Both trace to the requirements in `PROJECT.md`.
