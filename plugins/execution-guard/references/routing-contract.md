# Routing contract

For a substantive task, apply `pragmatic-core`, then select at most one current-phase specialist unless two independent failure modes are evidenced. `prove-it` is the only completion authority. Optional `quality-contract` and `handoff` are inactive without their triggers.
