# Artifact system

Use `shared/artifact-contracts/ARTIFACT-CONTRACT.md`. Traceability is opt-in and follows: `REQ -> ADR -> UX -> TASK -> change -> TEST/evidence`. Do not generate a document simply because a possible filename exists.
